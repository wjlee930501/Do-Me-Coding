# Build Evidence — DMC v1 M10: Final Docs + v1.0 Identity + Release Checklist (2026-07-08)

Runs: build run `dmc-run-e683f0168cfc` (21 grants, state_hash `638e4b79…`, plan_hash `1ab946e8…`)
→ release run `dmc-run-bef12a3d3345` (Rev 2.1 arming correction: the SAME scope minus the single
`.harness/evidence/` create grant that triggered the v0.2.6 G2↔G3 catch-22; 20 grants, state_hash
`82b3cf38…`, plan_hash `d2224f2a…`). Plan `.harness/plans/dmc-v1-m10-final-docs.md` Rev 2 APPROVED
(critic r1 NEEDS_CLARIFICATION → fold → r2 APPROVE 0 blockers; human plan gate 2026-07-08:
CF14=option (b), D1=document-not-harden, `.harness/evidence/` originals=KEEP, AGENTS.md=regenerate,
provenance tags=historical annotations, version verb=NO/v1.1). The first run's readiness FAIL
(gate-checks G3 then G2 on the evidence grant) is preserved in its run archive — a structural
allowlist finding, not a build defect; no file was hidden or shuffled to game the gate.

## What shipped (DMC-T016.1–.6)

5 synchronous scoped executors (Opus ×2: T016.1 identity, T016.3 new docs; Sonnet ×3: T016.2
consistency, T016.4 traceability, T016.5 MILESTONES) + the orchestrator lane (T016.6):

- **T016.1 (Opus)** — v1.0 identity refresh across 8 files: DMC.md (v1.0 banner; rules 5/6
  rewritten truthfully; 4 section tags → "(v1.0; introduced in v0.1.x)"; "## v1.0 Scope" reflects
  the shipped tree), CLAUDE.md (host-shipping file — same treatment, non-negotiables preserved),
  AGENTS.md **regenerated** via `dmc agents-md` (real HEAD orientation + landmark inventory;
  `--validate` VALID; stale `dmc-v0.1-scaffold` branch gone), CONTEXT_MAP.md (v1.0 title +
  "user-private" mislabel fixed), OMC_COEXISTENCE.md, both HOST_REPO policies,
  NOTION_EXPORT_SUMMARY.md (:13 + one disclosed extra line :15 — leaving it present-tense would
  have shipped a FALSE v1.0 claim; reframed as founding-decision provenance).
- **T016.2 (Sonnet)** — RUNTIME_ARCHITECTURE.md (Status DESIGN→IMPLEMENTED; §P20 stale "Codex has
  no Stop hook" corrected to the M6.5-verified advisory-parity reality), ORCHESTRATION_MODEL.md
  (Status; §4 standard-implementation reconciled to models.json), FABLE_WORKFLOW_TRANSFER.md
  (column relabeled as Phase-0/1 audit snapshot + shipped-reality pointer; row bytes unchanged).
- **T016.3 (Opus)** — NEW docs/DMC_V1_ENFORCEMENT_MATRIX.md (harness-matrix.json narrative, one
  harness per line, 5-tier surface table, SKILL.md:35-38 verbatim, CI count 15 blocking = 13
  substantive + 2 porcelain / 1 advisory), NEW docs/DMC_V1_HONEST_SCOPE.md (3 named topics + full
  residual register + CF14 named-tool posture + D1), NEW docs/DMC_V1_RELEASE_CHECKLIST.md
  (row-for-row 9 sub-gates + P17 items + CF14 posture; out-of-band binding — composer untouched);
  release-readiness.schema.md M10-extension bullet reworded (single hunk); dmc-ci.yml comment-only
  pointers (3+/1-).
- **T016.4 (Sonnet)** — NEW .harness/verification/dmc-v1-runtime-upgrade.md: "Audit Blocker
  B1–B10" traceability table (10 rows, FABLE B1-B13 disambiguated, dmc.verification.v1 VALID);
  corrected one brief error itself (B10 = `dmc radius`, not a nonexistent verb); CF14/D1/KEEP
  dispositions recorded.
- **T016.5 (Sonnet)** — docs/MILESTONES.md "## v1.0 — DMC v1 Runtime Upgrade (M1–M10) — CLOSED
  (2026-07-08)" appended (89 insertions, 0 deletions — pure append; stale v0.6.6-v0.6.9 "Next:"
  pointer superseded inside the new entry).
- **T016.6 (orchestrator)** — master plan §Approval Status M10 line updated (scope-granted);
  RUNTIME_ARCHITECTURE.md:387 dangling "DMC.md v0.1 exclusions" → "v1.0 Scope" (in-scope
  consistency touch-up found in the broad-grep review); release-run evidence set + full gate
  (results below).

## Product-identity acceptance (VC2 + broad-grep allowlist review)

VC2 (fenced, pipe-free): **pre-edit NON-EMPTY (12 hits — non-vacuous proven; critic-r1 BL1) →
post-edit EMPTY (exit 1)**. Broad `grep -rInE 'v0\.1' DMC.md CLAUDE.md AGENTS.md docs/` residual
review — every hit explained (zero unexplained):

| Class | Instances | Rationale |
|---|---|---|
| "(v1.0; introduced in v0.1.x)" annotations | DMC.md:69/89/105, CLAUDE.md:27/39, policies:1 | ratified provenance style — historical, not identity |
| Founding/translation provenance | NOTION:15, NOTION:28 | founding-decision + historical translation map |
| Pinned frozen-tool filenames | AGENTS.md:148/224 (dmc-v0.1.3-verify.sh etc.) | on-disk names, never renamed |
| Historical gap narrative | RUNTIME_ARCHITECTURE.md:343 | describes the closed pre-v1 state |
| Archival/analytical (allowlist) | DMC_REAL_REPO_PILOT_REPORT.md (v0.1.2 report), MILESTONES.md historical entries, COMPETITIVE_GAP_LEDGER.md, HARNESS_LANDSCAPE_2026.md + HARNESS_BENCHMARK_CARDS_2026.md ("DMC v0.1–v0.5" analytical ranges — critic-r2 AD-r2-3 category), `_DMC_*.md` + scaffold zip (bootstrap provenance) | point-in-time records; not live identity claims |

## Codex-honesty acceptance (VC4)

Forbidden-lexeme grep (all 7 doctor lexemes, word-boundary, `/codex/i` lines of the 3 new docs) =
EMPTY; required substrings in ENFORCEMENT_MATRIX: "ADVISORY" ×5, "pre-commit/CI" ×5. AD-r2-1 note
(word-boundary vs doctor substring semantics) stated in the matrix doc; AD-r2-2 one-harness-per-line
respected.

## Suites (post-build, live tree)

default selftest 9 sections 0 FAIL · m8-suite 126/0 (83+17+16+10) · agents-md 24/0 ·
release-gate 39/0 · mirror-check PASS (55 byte-identical) · linkcheck clean (24 files).
Committed-replica + post-commit live `selftest --all` (legacy 802/3/3 EXACT) recorded at closure below.

## Stray-file hygiene PROPOSAL (proposal ONLY — execution requires its own human approval)

Per master :97/:352 and audit B8 (by-design NO-ACTION at v1.0). Candidates, classified:

1. **Tracked bootstrap strays (candidate-for-removal, separate approval):**
   `_DMC_CODEX_IMPLEMENT_FROM_SCRATCH_PROMPT.md`, `_DMC_CODEX_PROMPT_AFTER_UNZIP.md`,
   `_DMC_IMPORT_GUIDE.md`, `_DMC_MANIFEST.md`, `do-me-coding-v0.1-scaffold.zip` (41KB zip in git),
   `dmc-glm-smoke`. Proposal: remove in a v1.1 hygiene commit after confirming nothing references
   them (linkcheck-verified); keep `_DMC_IMPORT_GUIDE.md` if provenance value outweighs noise.
2. **`.before-dmc` snapshot trees (KEEP until the M3 rollback guarantee is retired):**
   `.claude.before-dmc/**` (16), `.harness.before-dmc/**` (11), `AGENTS.md.before-dmc`,
   `CLAUDE.md.before-dmc` — these are the pre-DMC restore surface proven at M6 closure; removal
   only alongside a deliberate rollback-policy change.
3. **Untracked local run residue (local-only by policy, carry-forward 6):** `.harness/evidence/
   dmc-run-*.md` auto-logs + `.harness/runs/dmc-run-*/` + 3 orphaned `dmc-v1-m3/4/5-20260706.md`.
   Proposal: extend the DMC `.gitignore` block with `.harness/evidence/dmc-run-*.md` +
   `.harness/runs/dmc-run-*/` so a future `git add -A` cannot capture them; delete the 3 orphans
   locally at the operator's discretion.

## Release run + full gate (T016.6)

Sequence (verifier-corrected wording — this paragraph describes the PLANNED steps executed AFTER
the independent verifier's report, which is the release `verification_ref` precondition): mint the
green evidence set in `.harness/runs/dmc-run-e683f0168cfc/` (verify-plan.json + receipts for
CHK-M10-DOCS/SUITES/BASELINE + findings.json (closure-clean) + goal-ledger.json +
decision-record.json + approvals.jsonl: plan_approval + release with verification_ref →
`.harness/verification/dmc-v1-m10-release.md` VALID) → stage exactly the scope.lock files[] →
run the full gate. EXECUTED on the Rev 2.1 release run: findings ALLOW · goal ALLOW · decision
ANSWERED · 3 receipts minted (CHK-M10-DOCS/SUITES/BASELINE) · approvals.jsonl VALID (2 records,
chain intact, CF2 verification_ref → VALID report) · 20 staged == allowlist.
**`dmc gate release --full --run-id dmc-run-bef12a3d3345` → verdict PASS, exit 0 — all NINE
sub-gates PASS (approvals, chain, decision, diff-scope, findings, gate-checks, goal,
landmark-flag, receipts), flags [] (the landmark-authorized grants pre-cleared the FLAG).**
First-run readiness FAIL (G3→G2 evidence-grant catch-22) archived at
`.harness/runs/dmc-run-e683f0168cfc/release-readiness.json`.

## Post-sign-off regression caught by the frozen audit tier (AC6) — fixed + re-gated

The first committed-replica `selftest --all` came back 799/6: beyond the pinned 3 + the known
v0.3.2-AC4 clone artifact, TWO new FAILs — `dmc-v0.4.7-context-audit` (6/1) and
`dmc-v0.4.9-autonomous-dry-run` (8/1, a pure stage-0 cascade of the former). Root cause: the
REGENERATED AGENTS.md dropped the discoverability pointers to `AUTONOMY.md` + `docs/CONTEXT_MAP.md`
that the frozen v0.4.7 AC6 requires (`bin/lib/dmc-v0.4.7-context-audit.sh:51-53`) — exactly the
regression class that audit exists to catch, and one the m-suite/VC checks do not cover. Fix
(in-scope, run resumed): a 3-line "Companion context docs" pointer added to AGENTS.md §7;
`dmc agents-md --validate` still VALID; live re-run v0.4.7 = 7/0 and v0.4.9 = ACCEPTED (stage 0
REGRESSION green). AGENTS.md re-staged, stale readiness removed, full gate re-run:
**`dmc gate release --full --run-id dmc-run-bef12a3d3345` → PASS, 9/9 sub-gates, on the corrected
bytes.** The 799/6 replica output is superseded by the post-fix replica re-run (recorded below).

## Closure verdicts

- Independent verifier: `.harness/verification/dmc-v1-m10-release.md` — Final Status PASS.
- Critic r3 build sign-off: `.harness/evidence/dmc-v1-m10-critic-r3.json` — APPROVE, 0 blockers,
  3 low advisories (incl. the legitimate flags[]-CLEAR note on the post-edit re-arm).
- Committed-replica `selftest --all` (post-AC6-fix): `aggregate: tools=49 PASS=801 FAIL=4 N/A=3`
  — EXACTLY the known /tmp-clone baseline (pinned v0.1.3×1 + v0.2.3×1 + v0.3.2 AC5, plus the
  v0.3.2 AC4 clone-environment artifact documented at the audit remediation with a pristine-clone
  delta of ZERO). The M10 changes add NO regression to the frozen baseline; both AC6-regression
  FAILs (v0.4.7 / v0.4.9) are cleared. Definitive 802/3/3 proof = the post-commit LIVE dev-tree
  run (M9 pattern), recorded below.
- Live post-commit `selftest --all`: _recorded post-commit_.
- CI: _recorded post-push_.
