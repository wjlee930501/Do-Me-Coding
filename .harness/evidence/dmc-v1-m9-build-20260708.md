# M9 Build Evidence — Release Gate Composition + CI + E2E Dry Run

Run: dmc-run-25ecbe729a18 · Plan: `.harness/plans/dmc-v1-m9-release-gate.md` (Rev 2, APPROVED
wjlee 2026-07-08, plan_hash pre-approval `b90722a6…5a40a`) · Arming HEAD:
`a31846897f01ff3a8ec5c4a96efc973ea8921232` · Date: 2026-07-08 · Milestone: M9 (DMC-T014 +
DMC-T015).

## Tasks built

| Task | Owner (model) | Files | Result |
|---|---|---|---|
| DMC-T014.1 | executor (Opus) | bin/lib/dmc-release-gate.py (NEW), .harness/schemas/release-readiness.schema.md (NEW), .harness/schemas/delegation.schema.md (2 surgical additions) | `[release-gate] 39 PASS / 0 FAIL` |
| DMC-T014.2 | executor (Sonnet) | bin/dmc (gate verb + run_m9_suite + release-gate/m9-suite sections) | linkcheck clean 24; default 75/0 frozen |
| DMC-T014.3 | executor (Sonnet) | .github/workflows/dmc-ci.yml (NEW) + sentinel-step follow-up | YAML OK; step greps empty today |
| DMC-T015.1 | executor (Opus) | tests/fixtures/host-node/** (NEW), tests/fixtures/m9/** (NEW) | test-release-gate 56/0, test-e2e-loop 35/0 |
| DMC-T015.2 | orchestrator lane | INSTALL_MANIFEST.md (+2), this evidence, verification report | manifest +2 exact; m8-suite drift 10/0 |

## The composer (T014.1)

`bin/lib/dmc-release-gate.py` — the P18 full-tier composer. `release --full --run-id RID
[--root] [--base SHA] [--out]` runs NINE sub-gates (diff-scope · gate-checks(v0.2.6) ·
receipts(v0.6.2 coverage+validate) · findings(v0.6.3) · goal(v0.6.4) ·
decision(v0.6.5 decision-trace) · approvals(CF2 verification_ref→artifact resolution) ·
chain(M7 delegation check + worker apply-check, activity-scoped) · landmark-flag(P2)), each
normalized to `PASS|FAIL|MISSING|FLAG`; overall FAIL>PARTIAL>PASS with FLAG never degrading;
writes `dmc.release-readiness.v1` to `runs/<rid>/release-readiness.json`; exit 0 PASS / 1
FAIL|PARTIAL / 2 usage / 3 structural REFUSE. `release --quick` = subprocess delegation to
dmc-stop-gate.py (exit 0→0, 4→1) — the M6 stop hook stays on `stop-gate quick` (frozen). 39
self-test rows cover each sub-gate PASS+FAIL/MISSING, PARTIAL-never-PASS, FLAG-doesn't-fail,
structural REFUSE on tampered run.json/snapshot, --quick both directions, exit-code
normalization, overwrite refusal, determinism. `RGATE-*` value-blind reason codes.

Executor-disclosed composer deviations (verifier to confirm as sound, not weakening):
1. `--quick` delegates to `dmc-stop-gate.py <flags>` WITHOUT the literal `quick` token
   (bin/dmc strips it; the tool's argparse takes flags only) — behavior-identical to
   `dmc stop-gate quick`, no drift.
2. `chain` delegations-branch: `dmc delegation check` resolves its run dir via the tool's
   own repo_root() (no --root); production-correct (root==repo_root) and consistent under
   the copy-surface E2E.
3. scope.lock tamper surfaces as `diff-scope` FAIL `RGATE-SCOPE-LOCK-TAMPER` (a state-hit),
   NOT a structural REFUSE — the plan pins structural REFUSE to run.json seal / snapshot
   mismatch only (matching postbash-diff layer-B).

## Schema additions (T014.1)

`.harness/schemas/release-readiness.schema.md` (NEW) — dmc.release-readiness.v1: 9 sub-gate
names/enums, PARTIAL-never-PASS, chain ACCOUNTABILITY/PROVENANCE-tier disclosure
(append-log exemption; mutation floor = diff-scope + Ring-1 postbash), diff-scope honest
tier (names-only, --base, committed-diff blindness), CF2 rule, FLAG-is-review, gate-checks
staged-input precondition (Rev 2/A1), exit map, M10 checklist extension point, consumers
note.
`.harness/schemas/delegation.schema.md` (2 additions, CF13d): (a) the compact-canonical
serialization + LF-excluded prev_hash disclosure line for external chain authors; (b) the
`scope_lock_ref` field in the illustrative JSON (validator has required it since M5;
illustration catches up — behavior unchanged). `dmc selftest delegation` stays 41/0.

## CI (T014.3)

`.github/workflows/dmc-ci.yml` — one ubuntu-latest job, `on: [push, workflow_dispatch]`,
`timeout-minutes: 25`, `actions/checkout@v4` `fetch-depth: 0` (the m6 restore/rollback
fixtures `git show` the pinned pre-M6 commit `2999870…`; depth-1 breaks them). Steps:
bash -n floor · porcelain PRE · mirror-check · doctor · selftest --all (802/3/3 EXACT
comparator + every section 0 FAIL) · model-name grep (CF3-scoped: bin adapters
.claude/install orchestration .claude/agents, --exclude models.json + dmc-roles.py) ·
forbidden-lexeme/network grep (AA1 MANDATORY byte-exact M8 :507 pattern
`\b(nc|netcat)\b|curl|wget|urllib|http\.client|socket|requests|smtplib|ftplib|api\.|
CODEX_API_KEY|GLM_API_KEY|dangerously-bypass-hook-trust` over `.claude/install` +
`bin/lib/dmc-doctor.py` ONLY) · Codex wiring presence · porcelain POST. Top comment carries
the Option A boundary (advisory shims; CI = the Codex enforcement boundary; no parity
claim). Both greps return empty on HEAD today (verified redirect-free).
Sentinel-step follow-up: the initial step asserted `.codex/.dmc-created` present+tracked,
but that sentinel is a HOST-install provenance marker (dropped only when the installer
CREATES `.codex`); this dev repo's `.codex` is milestone-authored (only config.toml +
hooks.json tracked). Amended to assert `.codex/{config.toml,hooks.json}` exist + tracked;
the sentinel's provenance behavior is proven by the m8-suite fixtures inside `--all`.
nullglob guard on the bash -n step (adapters/codex holds only .py shims today).

Note (disclosed sequencing): "CI green on branch" is the ONE acceptance criterion
verifiable only AFTER the human push gate — the closure record cites the workflow run
conclusion. Local dry-runs of every CI grep step pass on HEAD today.

## Fixtures (T015.1)

`tests/fixtures/host-node/` — committed minimal node-host shape (package.json host-app,
README, .gitignore, src/index.js + src/util.js). Never armed in place; the E2E copies it
into a mktemp repo (copy_surface).
`tests/fixtures/m9/` — `_m9common.sh` (m6/m7/m8 conventions: sourced-only, porcelain
guard, mktemp+trap, copy_surface, self-set git identity, offline, no-secrets) +
`test-release-gate.sh` (56/0) + `test-e2e-loop.sh` (35/0).

Green path (test-release-gate.sh): all 9 sub-gates PASS (diff-scope · gate-checks ·
receipts · findings · goal · decision · approvals · chain via a real
authorize+delegation-append chain `RGATE-CHAIN-PASS` · landmark-flag empty) → verdict PASS,
release-readiness.json validates. Staged the scope.lock files[] == modified/new set (AA3).
Seeded gaps: g1 out-of-scope⇒diff-scope FAIL · g2 force-staged excluded-evidence⇒gate-checks
FAIL · g3 uncovered check_id⇒receipts FAIL · g4 blocked finding⇒findings FAIL · g5 broken
goal trace⇒goal FAIL · g6 unresolved decision link⇒decision FAIL · g7 verification_ref→ghost⇒
approvals FAIL (RGATE-VERIFICATION-REF-UNRESOLVED, CF2 teeth) · g8a tampered delegations⇒chain
FAIL (DELEG break) + g8b missing auth members⇒chain FAIL (RGATE-CHAIN-MEMBER-MISSING) + g8c
direct apply-check on deleted authorization⇒exit 3 WAUTH-MISSING-AUTH · g9 contract-landmark
change⇒FLAG, verdict stays PASS · g10 findings removed⇒PARTIAL exit 1 · g11 tampered run.json⇒
structural REFUSE exit 3 (RGATE-RUN-STATE-INVALID) · g12 no worker activity⇒chain
PASS-with-note. --quick alias: RUNNING blocked⇒exit 1, SUSPENDED⇒exit 0, latency ~0.10s <2s.

E2E full loop (test-e2e-loop.sh, 35/0): orient→landmarks→plan(APPROVED)→critic verdict
APPROVE (plan-hash-bound)→verdict gate PASS→run start→scope-lock compile→one benign
in-scope edit→ONE DENIED ATTEMPT PER CANONICAL-FIVE CLASS (bash out-of-scope · scope.lock
self-edit · secret-glob · JWT worker-result REJECT · rename-to-forbidden REJECT — all
value-blind)→acceptance/verify-plan compile→receipts minted (five-type evidence_type,
AA4)→one fixloop append→run suspend→stop-gate quick PASS (suspended)→run resume→verification
report (validates)→release approval→git add scoped set→gate release --full PASS→human-gate
record present+validated→LATENCY rows: stop-gate quick <2s AND gate release --quick <2s.
Live repo porcelain-unchanged throughout.

Executor-disclosed T015.1 nuances (verifier to confirm): (1) gate-read artifacts are
materialized directly in their schema shapes (the identical methodology
dmc-release-gate.py's own --self-test uses) rather than driving the fragile planner
pipeline — exercises the gate's real required_checks() contract, not a weakened assertion;
(2) the chain sub-gate iterates EXISTING authorization files, so its realizable chain-FAIL
reasons are RGATE-CHAIN-DELEG-FAIL + RGATE-CHAIN-MEMBER-MISSING (g8a/g8b); g8c proves the
literal WAUTH-MISSING-AUTH floor via a DIRECT apply-check — a plan-text nuance, not a bug;
(3) armed sandbox gitignores `.harness/` so run-state + append-only worker/delegation logs
(read by path, never by git) don't surface as diff-scope changes — diff-scope still has
teeth (g1 out-of-scope + g2 force-staged excluded caught).

## Section results (live, working tree)

- `bin/dmc selftest release-gate` → `[release-gate] 39 PASS / 0 FAIL`
- `bin/dmc selftest m9-suite` → test-release-gate 56/0 + test-e2e-loop 35/0, aggregate exit 0
- `bin/dmc selftest delegation` → 41/0 (schema doc edit inert to the validator)
- `bin/dmc selftest m8-suite` → manifest-drift 10/0 post-regen (emitted == committed
  byte-for-byte)
- `bin/dmc linkcheck` → clean, 24 files, the new `gate` verb resolves
- `bin/dmc mirror-check` → PASS (the five composed legacy tools byte-unchanged)
- `bin/dmc selftest` (no-arg default) → 75/0, frozen 9 sections unchanged
- INSTALL_MANIFEST.md regen diff = EXACTLY +2 (`bin/lib/dmc-release-gate.py`,
  `release-readiness.schema.md`); CI workflow + fixtures are dev/CI surface, correctly NOT
  in the ship manifest.

## Committed-replica selftest --all (working-tree-drift proof, carry-forward #7)

Replica = copytree(HEAD working tree, excl .git + .harness/runs archives) → git init →
single commit → `bin/dmc selftest --all`:
- Legacy aggregate: `tools=49 PASS=802 FAIL=3 N/A=3` EXACT == pinned M3 baseline;
  originals-alone reproduce 802/3/3; `SELFTEST-ALL RESULT: PASS`.
- The 3 pinned FAILs are the human-accepted baseline (v0.1.3 GLM-grep, v0.2.3 V5,
  v0.3.2 AC5 — carry-forward #1), never masked.
- Lone extra FAIL: `[FAIL] restored byte-identical to pinned: .claude/settings.json`
  (rollback/restore) — the SAME squash-replica git-history artifact recorded at M7/M8: a
  single-commit replica lacks the pinned pre-M6 commit `2999870`, so the restore-to-pinned
  cmp has no object to restore. M9 touched NEITHER settings.json NOR the rollback machinery
  (tests/fixtures/m6/) — confirmed by the diff. Definitive proof = the post-commit live
  `--all` on real history (closure step).

## Scope conformance

All 17 implementer files ⊆ scope.lock (run dmc-run-25ecbe729a18, 17-entry lock, bounds
max_files 17 / max_added 6500 / max_deleted 120). The one changed file outside scope.lock —
`.harness/plans/dmc-v1-runtime-upgrade.md` — carries only the §Approval Status M9 record
(+7/−3, orchestrator lane, plan row L82 authority). No frozen surface touched: the five
composed legacy tools (mirror-check green), M6 hooks + settings.json, providers,
installer/uninstaller/doctor code, root WORKER_* schemas — all absent from the diff.
