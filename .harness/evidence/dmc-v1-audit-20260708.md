# DMC v1 Full-Project Audit — 2026-07-08

Pre-M10 consolidation audit of the entire branch `claude/dmc-v1-runtime-upgrade-c5uch1`
(HEAD `9ae8682`, = main + M1–M9). Orchestrated read-only fan-out: 7 parallel auditors
(Opus 4.8 ×3 on the enforcement/security/lifecycle core, Sonnet 5 ×4 on the legacy suite,
schemas/config, docs/adapters/CI, fixtures/snapshots/manifest). Surface: 588 tracked files,
~47k LOC py/sh. No mutations made during the audit; no secret-file contents read.

## Verdict

The enforcement core is fundamentally sound: scope-lock adjudication (exact match, `..`/abs/
symlink refusal, state-hash tamper), postbash-diff root-of-trust sealing, the M9 release-gate
composer precedence (FAIL>PARTIAL>PASS, no PARTIAL→PASS leak), delegation hash chains,
worker "propose-never-mutate" invariant, and the glm-api mock-first/no-live-in-build invariant
all HOLD and are self-test-green. No live-provider path, no auto-apply path, and no key-print
path exists in build/verify. Findings are hardening/consistency/doc-accuracy gaps, not a broken
core. Two NEW carry-forwards for M10 surfaced (D1 md5 false-PASS; plus the already-tracked CF14).

## Fix tiers (proposed scope for this pre-merge pass)

### Tier 1 — SAFE minor fixes (non-frozen, non-protected, low blast radius)

| ID | File | Fix | Sev |
|----|------|-----|-----|
| B1 | bin/lib/dmc-delegation.py | add the `service-account.json` branch to `is_secret_path` (10 sibling tools have it; delegation is the sole omission) | MED |
| B2 | bin/lib/dmc-delegation.py | reject `..` segments in `resolve_scope_lock_ref`/`_load_json_path_safe` before join (release-gate's `safe_repo_rel` already does) | LOW |
| D4 | bin/lib/dmc-legacy-selftest.py | remove/annotate the no-op `--check` flag (accepted but never branched on) | LOW |
| D7 | bin/dmc | `cd` to repo root in the `legacy)` verb dispatch (tools assume cwd=root; aggregator already pins it) | LOW |
| E2 | .github/workflows/dmc-ci.yml | add `.harness/schemas` to the CF3 model-name grep path list (currently unscanned; clean today) | MED |
| E4 | orchestration/roles.json | reword `strategic-orchestrator.session_binding` prose to acknowledge read-only explorer/planner facets (descriptive only; roles.py doesn't validate content) | LOW |
| F1 | .claude/agents/{critic,explorer,planner,verifier,release-auditor}.md | fix the now-false "Ring-1 … arrives in M6" → "enforced since M6" (M6 shipped `d721487`; pre-tool-guard.sh already wires `dmc bash-radius`) | MED |
| F2 | docs/CODEX_ADAPTER.md | replace "nothing here is built until the M6.5 plan clears its gate" framing with "Status: IMPLEMENTED (M6.5, Option A advisory)" | MED |
| F6 | docs/DMC_AGENT_HANDOFF.md | bump/drop the stale "(v0.2.5)" title tag (body already defers to orchestration/roles.json) | LOW |
| F8 | .github/workflows/dmc-ci.yml | drop the dead `adapters/codex/*.sh` glob in the bash -n step (that dir has only .py) | NIT |
| G2 | .gitignore (root) | add the secret-class pattern set (`.env*` except examples, keys/certs, credential files, `.ssh`/`.aws`/`.gnupg`) — dev repo currently has ZERO secret patterns | HIGH |

### Tier 2 — security hardening on PROTECTED M6 hook surfaces (needs m6-suite re-verify)

| ID | File | Fix | Sev |
|----|------|-----|-----|
| C1 | .claude/hooks/pre-tool-guard.sh | broaden the Bash secret-read denylist beyond `cat`/`printenv` to `head/tail/less/xxd/od/strings/base64/awk/sed/grep/rg/cp/tee …` targeting `.env`/`~/.ssh`/`~/.aws`/`*.pem`/`*.key` (closes `cp .env x`→`Read x` + `head .env` two-step bypass) | HIGH |
| C2 | .claude/hooks/evidence-log.sh | extend `redact()` to the shared token set (AKIA/JWT `eyJ`/`xox`/`gh[opsu]_`/`ya29`/PEM/`Bearer`/`Authorization:`) so command-line secrets aren't persisted plaintext to `.harness/evidence/<run>.md` | MED |
| A3 | .claude/hooks/evidence-log.sh | on a post-Bash detector crash (exit ∉ {0,4}) record a soft block via `dmc run block` instead of passing silently (fail-closed) | MED |

### Tier 3 — improvements needing a call (CI strengthening / ship-surface)

| ID | File | Fix | Sev | Note |
|----|------|-----|-----|------|
| F7 | .github/workflows/dmc-ci.yml | promote `selftest {m6-suite,m65-suite,m7-suite,m8-suite}` to individual BLOCKING steps (they're hermetic + non-frozen; only the ancient v0.2.x tools need the advisory tier) | LOW | must confirm green on the runner |
| G1 | .claude/install/dmc-install.sh + INSTALL_MANIFEST.md | ship the 6th canonical agent `release-auditor.md` (declared in roles.json + orchestration doc, but installer's `AGENTS=` lists only 5) + regen manifest | HIGH | ship-surface; needs install-suite re-verify |

## DEFER-M10 (bigger / contract-class / frozen-surface / already-tracked)

- **D1 (NEW carry-forward, HIGH):** ~20 FROZEN legacy tools use bare BSD `md5` unguarded in their "repo byte-unchanged / no-drift" assertions; on any host without `md5` (GitHub ubuntu ships only `md5sum`), the check degrades to `"" = ""` and reports a **silent false-PASS** (v0.3.1 has the inverse: a false-FAIL from an ordering bug). Distinct bug class from CF14 (false-PASS vs pyc-litter). The suite's own `dmc-v0.4.9`+`v0.5.0+` tools already fixed this prospectively (`HASH_CMD`/`python3 hashlib`) but never backported. Cannot fix without breaking the mirror-pin — record as a distinct M10 carry-forward. Catalog: see Lane D report.
- **CF14 (confirmed):** 6 legacy tools' `python3 -m py_compile` litters in-tree `__pycache__` that trips later tools' porcelain-cleanliness assertions (v0.2.1:47 / v0.2.3:89 / v0.6.0:148 / v0.3.9:194). CI mitigates via `PYTHONPYCACHEPREFIX=/tmp`; local `--all` manifestation remains. M10-owned.
- **A1 (MED, security):** the L0 always-on `git apply`/`patch` deny + L1 armed write-radius are evadable via `eval …` and `$(…)` command substitution (the inline grep anchors backtick but not `$(`); postbash-diff scope check backstops only when armed+active. Fixing means editing dmc-bash-radius.py + pre-tool-guard.sh + reconciling the W-series self-tests — a focused security-hardening plan, not a minor edit.
- A2/A4/A5/A6 (secret-directory Grep, run-dir self-authorable stop-gate inputs, no-python fail-open on the Bash surface, stop-gate quick-tier latency at scale).
- B3 (RUNNING→DONE unreachable — no `complete` verb), B4 (apply-check doesn't re-run check_review), B5 (checkpoints `--validate` structure-only).
- C3 (glm-api adapter doesn't token-scan output like oauth-cli), C4 (secret-guard no symlink resolve), C5 (secret-filename coverage misses `prod.env`/`*.ppk`/`*.p8`/`*.asc`/`*.gpg`/bare `credentials`), C6/C7 (placeholder heuristic; `--out` target parity) — several touch the md5-identity-pinned `secret-paths.sh` triple, needing coordinated change.
- E1 (instance-validate leaves 7 documented enums unenforced), E3 (linkcheck scan-surface excludes schemas/docs cross-refs).
- F3/F4/F5 (DMC.md/CLAUDE.md/AGENTS.md/architecture docs v0.1→v1.0 identity staleness — already the M10 identity-refresh deliverable; note F3 predates the v1 work).
- G3 (host-shipped `.gitignore` block narrow), G4 (`.harness/workers/authorizations/` not in HARNESS_DIRS — already handoff carry-forward (e)), G5 (orphaned M2/M4 fixtures).

## NO-ACTION (verified clean or by-design)

Model-name single-source (verified sound), skill-mirror sync (no drift), manifest byte-accuracy
(emitted == committed), mirror-check green (55-file), rollback fixtures (pinned-commit half 100%
green; the standalone 23/7 divergence is by-design), the 3 human-accepted pinned FAILs, `.before-dmc`
frozen snapshots.
