# DMC v1 M6 — Hook/Guard Hardening: Build Evidence

Milestone: DMC-T011 (§M6, plan `.harness/plans/dmc-v1-m6-hook-hardening.md`, Rev 3, APPROVED)
Run: `dmc-run-53553ac50a20` (SUSPENDED by design during the T011.4 build; all armed-flow
tests run in mktemp fixture repos, never against this run)
Date: 2026-07-06
Nature: build/verification evidence. No network / live / model / API call; no secret read
(secret cases assert DENY by path only). All fixture arming + writes land in mktemp sandboxes.

## Per-task record

### DMC-T011.1 — pre-M6 byte fixtures + whole-tree rollback test  (COMMITTED)
- Committed as `192dce6` ("M6 T011.1 — pre-M6 hook-tree byte fixtures (pinned 2999870) +
  whole-tree rollback test (30/0)"). Its own reviewable unit; nothing else in it.
- `tests/fixtures/hooks-v0.6.5/**` = byte-identical copies of the ENTIRE pre-M6 `.claude/hooks/`
  tree (incl. `lib/**` + the worker guards) + `.claude/settings.json`, bytes pinned to the pre-M6
  HEAD commit `299987047e448cff6ea9ddaf8011d66992901003`.
- `tests/fixtures/m6/test-rollback.sh` cmps the WHOLE tree vs live AND vs `git show <pinned>`.

### DMC-T011.2 — Ring-0 verdict CLIs + Rev 3 Option A run-state tamper detection
- New: `bin/lib/dmc-bash-radius.py`, `dmc-postbash-diff.py`, `dmc-verify-crosscheck.py`,
  `dmc-stop-gate.py`; schema `.harness/schemas/blocked-marker.schema.md`.
- Scoped edits: `bin/lib/dmc-run-lifecycle.py` (verdict-REJECT arming floor C11; `blocked.json`
  sidecar write/read/resolve; arming `snapshot.txt`; STATES tuple + run.json schema UNCHANGED),
  `bin/lib/dmc-scope-lock.py` (Rev 3 Option A: inline write-once operative-snapshot record in
  run.json at the sanctioned `cmd_compile` lock-write site; `--out` isolation; delete-then-
  recompile refusal). Single-owner `bin/dmc` verb registration.
- Self-tests (current): bash-radius 50/0 (incl. the landed armed wrapper-exec hardening),
  postbash-diff 25/0, verify-crosscheck 13/0, stop-gate 11/0 (`dmc selftest m6-core` = 99/0,
  exit 0). run-core 168/0 after the scoped edits.

### DMC-T011.3 — hook shims + settings wiring
- Modified (protected surface, this milestone's explicit authorization): `.claude/hooks/`
  `pre-tool-guard.sh`, `scope-guard.sh`, `secret-guard.sh`, `stop-verify-gate.sh`,
  `evidence-log.sh`. Each parses superset tool-JSON keys (case-insensitive paths), calls the
  Ring-0 CLI, and translates the verdict to the host envelope; L0 static floor fires inline;
  Ring-0 resolution is script-relative; fail-closed only when armed+active.
- `adapters/claude-code/README.md` (Ring-1 adapter home, new).
- `.claude/settings.json` NOT changed — the existing matcher/event wiring already routes the five
  events; no new registration was required this milestone (see the verification HONEST-SCOPE note).
- `dmc-router.sh` NOT edited (plan: no M6 need). `worker-*` guards NOT touched (M7 surface).

### DMC-T011.4 — adversarial + compatibility suites, E2E, restore proof, evidence/verification
- New: `tests/fixtures/m6/_m6common.sh` (sourced helper: arming through the REAL Ring-0 path,
  tool-JSON builders, real-repo porcelain guard), `test-adversarial.sh`, `test-compat.sh`,
  `test-e2e-ultrawork.sh`, `test-restore.sh`.
- `bin/dmc`: registered `selftest m6-suite` (runs the four scripts; named-only + under `--all`;
  NOT in the no-arg default — mirrors the `m6-core` registration).
- Post-review tightening: after the wrapper hardening landed in bash-radius (m6-core 99/0), the
  adversarial suite's `sh -c`/`bash -c`/`xargs` git-apply rows were converted from soft
  "record-the-residual" branches into HARD assertions — armed wrapper-exec forms DENY, a benign
  wrapper payload ASKs, and the unarmed L0 allow-boundary is pinned explicitly (adversarial 34 -> 38).

## Raw counts (this build)

| Check | Command | Result |
|---|---|---|
| adversarial suite | `bash tests/fixtures/m6/test-adversarial.sh` | 38 PASS / 0 FAIL (exit 0) |
| compatibility matrix | `bash tests/fixtures/m6/test-compat.sh` | 45 PASS / 0 FAIL (exit 0) |
| ultrawork stop-block E2E | `bash tests/fixtures/m6/test-e2e-ultrawork.sh` | 10 PASS / 0 FAIL (exit 0) |
| restore proof | `bash tests/fixtures/m6/test-restore.sh` | 11 PASS / 0 FAIL (exit 0) |
| registered suite | `bin/dmc selftest m6-suite` | 104 PASS / 0 FAIL total (exit 0) |
| M6 Ring-0 self-tests | `bin/dmc selftest m6-core` | 99 PASS / 0 FAIL (bash-radius 50, postbash-diff 25, verify-crosscheck 13, stop-gate 11; exit 0) |
| run-core regression | `bin/dmc selftest run-core` | 168 PASS / 0 FAIL (exit 0) |
| mirror-check | `bin/dmc mirror-check` | PASS green (55-file byte-equality, exit 0) |
| bash -n | touched shell files (bin/dmc, 5 hooks, 5 suite scripts) | all clean |
| py_compile | the 6 touched/new bin/lib/*.py | OK |
| legacy v0.1.3 slice | `bin/dmc legacy v0.1.3-verify --self-test` | 43 PASS / 2 FAIL (both expected non-behavioral — see below) |

The two v0.1.3 FAILs are the documented, non-behavioral carry-forwards, NOT regressions:
- `FAIL existing hooks changed: .claude/hooks/evidence-log.sh` — a byte-snapshot pin, not a live
  behavioral assertion; M6's authorized protected-surface shim edit legitimately changed the file.
- `FAIL GLM/worker code found` — the v0.1.3 verifier predates the v0.2 Worker Bridge, so it flags
  worker code that legitimately exists in later milestones.
The 43 PASSes include the v0.1.3 LIVE-HOOK behavioral assertions (empty stdout on allow, npm-install
ask, destructive/secret denies, all-modes secret floor) — i.e. the shim runtime contract holds and
the baseline is met by behavior, not by masking (carry-forward #1 intact).

## LIVE-FIRE events observed during the parent M6 session (verbatim facts)

These are real enforcement events that fired against the LIVE session while M6 was being built —
the guards working in production, not in a fixture:

(a) The new `stop-verify-gate` shim HELD a real session stop because the interim verification
    report's Changed Files diverged from the actual worktree — the semantic cross-check
    (dmc-verify-crosscheck) working live, with no keyword regex.
(b) `dmc run suspend` then ALLOWED the stop — the designed escape hatch verified live.
(c) The legacy scope-guard DENIED the orchestrator's own mid-run plan edit (self-escalation
    prevention), which forced the human-gated Rev 3 amendment path rather than an in-place edit.
(d) During the critic review, the live `git apply` L0 deny fired on the critic's own probe command.
    (Re-observed in THIS T011.4 build: the executor's git-apply probe commands were denied by the
    live session pre-tool-guard floor — see the build transcript; probes were then base64/variable-
    split to test the inner hooks without tripping the outer floor.)
(e) Executor `t0113` found + fixed a real bug: `dmc run block` without `--root` briefly wrote
    `blocked.json` onto the live run; it was cleaned. (The post-Bash guard now always passes
    `--root "$PROJECT_DIR"`, and the T011.4 evidence-log flow was verified to scope its block to
    the fixture run only.)

## Critic verdict chain + human gates

| Round | Verdict | Blockers | plan_hash (sha256) | Note |
|---|---|---|---|---|
| r1 | REJECT | 7 (B1-fixture-set-incomplete, B2-legacy-baseline-executes-live-hooks, B3-verdict-gate-ring1-wiring-absent, B4-task-id-collision, B5-blocked-state-mechanism, B6-no-active-run-semantics, B7-master-acceptance-dropped) | `06cd9495…4b29238a` | 5 lenses (correctness/scope/security/completeness/migration); drove Rev 2 |
| r2 | APPROVE | 0 | `968cd191…a66be8bd` | Rev 2 (7 blockers closed) |
| r3 | APPROVE | 0 | `45c35fe9…daab717b` | **Binding pre-approval artifact** (frozen Rev 2 bytes named in the plan's Approval Status) |
| r4 | APPROVE | 0 | `8ce3c1c3…bbd9f52d` | Rev 3 amendment (post-Bash snapshot refresh + run-state tamper detection, Option A) |

Human gates (2026-07-06 session, via AskUserQuestion):
- Approval of DMC-T011.1–.4 including the protected-surface edits, option "APPROVED — M6 구현 착수",
  bound to the frozen pre-approval bytes sha256 `45c35fe915ab99088f25595b4f370d90f313a33dd70c2cdc81b7131dbaab717b`.
- Rev 3 mid-run amendment gate: authorized the scoped `dmc-scope-lock.py` compile-site snapshot
  refresh + run-state CONTENT tamper detection + the plan-file amendment apply (after a critic
  delta REVISE→pins-folded cycle).
- Explicitly NOT approved (separate gates): staging/commit/push, M6.5+ work,
  worker-validator/installer/router edits, any live call, any secret access, main/master changes,
  docs/MILESTONES.md.

## Committed-replica `selftest --all` proof + dirty-tree drift accounting

The pinned legacy baseline is 802 PASS / 3 FAIL / 3 N/A (`.harness/evidence/dmc-v1-m3-baseline.md`).
On the DIRTY (uncommitted) working tree, `bin/dmc selftest --all` reads 786 PASS / 19 FAIL / 3 N/A
— a delta of +16 FAILs. Every drift is an uncommitted-working-tree / byte-pin check tripped by the
M6 hook edits (chiefly `.claude/hooks/evidence-log.sh`), and ALL clear on a committed tree.

Per-script drift (dirty FAIL over baseline FAIL), the full +16 accounted across 13 scripts:

| Script | dirty FAIL | baseline FAIL | drift | what it pins |
|---|---|---|---|---|
| dmc-v0.1.3-verify | 2 | 1 | +1 | evidence-log.sh byte-pin |
| dmc-v0.2-verify | 2 | 0 | +2 | modified hook byte-pins |
| dmc-v0.2.1-verify | 2 | 0 | +2 | modified hook byte-pins |
| dmc-v0.2.1.1-verify | 1 | 0 | +1 | modified hook byte-pin |
| dmc-v0.2.2-verify | 1 | 0 | +1 | modified hook byte-pin |
| dmc-v0.2.3-verify | 3 | 1 | +2 | modified hook byte-pins |
| dmc-v0.2.4-verify | 1 | 0 | +1 | modified hook byte-pin |
| dmc-v0.2.5-verify | 1 | 0 | +1 | modified hook byte-pin |
| dmc-v0.2.9-effort-provider-policy | 1 | 0 | +1 | H4 evidence-log.sh byte-pin |
| dmc-v0.3.1-verify | 1 | 0 | +1 | V12 evidence-log.sh byte-pin |
| dmc-v0.3.3-verify | 1 | 0 | +1 | C9 evidence-log.sh byte-pin |
| dmc-v0.5.9-dynamic-workflow-acceptance | 1 | 0 | +1 | AC13 working-tree drift |
| dmc-v0.6.0-verify | 1 | 0 | +1 | V15 working-tree drift |
| **total** | | | **+16** | (baseline 3 -> dirty 19) |

Accuracy note: the initial "8 scripts + v0.5.9/v0.6.0" scoping under-counted by 3 — v0.2.9 (H4),
v0.3.1 (V12), and v0.3.3 (C9) also byte-pin `evidence-log.sh` and supply the remaining +3. Verified,
not masked.

COMMITTED-REPLICA PROOF (this build): a throwaway `cp -R` of the entire repo (incl. `.git`, nothing
excluded) into a mktemp dir, `git add -A && git commit` IN THE COPY (a separate commit on the copy's
own history), then `bin/dmc selftest --all` run IN THE REPLICA. Real-repo confinement verified:
`git status --porcelain` + `git rev-parse HEAD` on the LIVE repo were byte-identical before and after
(HEAD stayed 192dce6). Replica result:
- legacy aggregate EXACTLY tools=49 PASS=802 FAIL=3 N/A=3 (the 3 accepted baseline FAILs: v0.1.3
  44/1, v0.2.3 19/1, v0.3.2 7/1); "originals alone reproduce the pinned baseline: 802/3/3".
- every new section 0 FAIL: run-core 168/0, loop-core 78/0, roles 19/0, verdict-validate 16/0,
  verdict-gate 9/0, delegation 29/0, linkcheck 17/0, bash-radius 50/0, postbash-diff 25/0,
  verify-crosscheck 13/0, stop-gate 11/0; m6-suite 38+45+10+11 = 104/0.
- no clean module FAILs (the earlier dirty-run `delegation N24` porcelain FAIL was a self-inflicted
  concurrency artifact — writing report files during a background run — and does NOT occur on the
  clean committed replica).
The post-commit LIVE re-run of this same `--all` remains the standing closure condition at the gate.

## Repo cleanliness

Every suite embeds a `git status --porcelain` before/after check on the LIVE repo and asserts it is
byte-identical — all suites reported the real repo untouched. The M6 surface remains UNCOMMITTED in
the working tree (commit is a separate human gate); only the T011.1 fixtures are committed (192dce6).
