# Verification Report

## Run ID

dmc-run-af50706d0402

(SUSPENDED; scope.lock armed and immutable. Third armed run of the v1.1.4 regen-pin cycle; supersedes dmc-run-4b0202a2f0b7 (and, before it, dmc-run-e8b6a347af41) for gating, folding the Rev 4 name-pin fix. Independently re-verified against APPROVED plan `.harness/plans/dmc-fable-core-regen-pin.md` Rev 4; no implementer transcript trusted — binding re-checked, the STAGED suite content read from the index, and the suites re-run in a fresh read-only lane.)

## Plan

`.harness/plans/dmc-fable-core-regen-pin.md` — fable-core follow-up (v1.1.4): committed==regenerated selftest pins for the generated artifacts. Binding chain independently verified (Rev 4):

- `shasum -a 256` of the plan = `d0156d70e85d6c8d39a23e463c99da944b79228131f2431ff81d476f3fbf4caf`
- == `run.json` `plan_hash` (`d0156d70…`) ✓
- == `scope.lock.json` `plan_hash` (`d0156d70…`) ✓
- == critic r4 `plan_hash` (`d0156d70…`) ✓ — verdict `APPROVE`, `lenses` [correctness, scope, security], `advisory: true`, `context_provenance: "fresh"`, `blockers: []`. (r3 `e905ca9c…` correctly superseded.)
- scope.lock `compiled_at_head` = `bbeb27702d23892a0482127996552f393b1fddad` == current HEAD == critic r4 `repo_hash` ✓ (HEAD is back at `bbeb277` after the soft-reset of local build commit `8451cc0`; the candidate is re-staged).
- Operative-snapshot integrity: computed `scope.lock.json` sha256 = `cad786d7b0c670a22…` == `run.json` `operative_snapshot.scope_lock_sha256` ✓; computed `snapshot.txt` sha256 = `867dee447f4618344…` == `operative_snapshot.snapshot_sha256` ✓.
- scope.lock `files[]` == EXACTLY the 3 candidate paths (`bin/dmc` edit/enforcement/`landmark_authorized`; `docs/MILESTONES.md` edit/release/`landmark_authorized`; `tests/fixtures/m6.5/test-agents-md-drift.sh` create/ordinary). Bounds `max_files 3` / `max_added 500` / `max_deleted 60`.
- run.json/scope.lock `repo_hash` = `74b29f6602d98ac41…` (arming-time content hash of the re-staged tree; both artifacts agree — distinct from the git HEAD commit `bbeb277`, as expected).

## Supersession (three-run chain — two honest failure records preserved)

- **Run 1 `dmc-run-e8b6a347af41` — honest G2 FAIL (defensive lock rows).** Rev-2 plan (`0c45ea98…`), 5-path lock. Preserved `.harness/runs/dmc-run-e8b6a347af41/release-readiness.json` records overall `FAIL`, sub-gate `gate-checks FAIL`: `"G2 FAIL approved files not staged: AGENTS.md INSTALL_MANIFEST.md"` — the two never-firing regenerate-IF-drift rows were unsatisfiable staging obligations (`sg_gate_checks` builds G2's allowlist from ALL lock rows). Deterministic gate correctly caught the plan defect; no masking.
- **Run 2 `dmc-run-4b0202a2f0b7` — gate PASS 9/9, then a post-gate REAL defect caught by the committed-replica --all leg.** Rev-3 plan (`e905ca9c…`), 3-path lock (the two conditional rows moved OUT: drift ⇒ HALT + follow-up scope). Preserved `.harness/runs/dmc-run-4b0202a2f0b7/release-readiness.json` = overall `PASS`, all 9 sub-gates PASS. BUT the subsequent committed-replica `selftest --all` verification leg (the AC-4 check) then caught a REAL candidate defect: the drift suite's POSITIVE byte-equality assertion fails in ANY checkout dir whose basename != `DMC`, because the generator titles `AGENTS.md` line 1 by the root basename. In CI the checkout dir is `Do-Me-Coding`, so the BLOCKING m65-suite step would have gone RED on push. This is the second honest failure record — recorded in plan Finding F8 + Notes Rev 4 (the replica evidence `1c1 < # AGENTS.md — DMC` from a replica dir). Replica-based verification working as designed (a live-tree lane, dir `DMC`, structurally could not have observed it — prove-the-work / no-fake-completion).
- **Run 3 `dmc-run-af50706d0402` (this run) — Rev 4 name-pin fix folded.** Rev-4 plan (`d0156d70…`), same 3-path lock, HEAD re-based to `bbeb277` after soft-reset of the pre-amend local build `8451cc0`; the content is re-staged with the fix. The executor edited ONLY the suite. Critic r4 APPROVE re-bound `d0156d70…`.

## Root cause — independently confirmed at source

- Committed `AGENTS.md` line 1 (read directly) = `# AGENTS.md — DMC`.
- `bin/lib/dmc-agents-md.py:173-174`: `# 1. Repo identity — name is the directory basename (derivable)` / `name = os.path.basename(os.path.abspath(root)) or "repository"`.
- Therefore a naive regen via `--root "$ROOT"` inherits the real checkout-dir basename into line 1; the positive byte-compare against the committed `— DMC` title FAILS in any dir != `DMC`. The Rev 4 fix normalizes ONLY this environment artifact (a name pin), not the content — a genuine title/content drift still fails byte-for-byte. Correct layer; not a mask.

## Changed Files

- `bin/dmc` (+3/−2, STAGED): `test-agents-md-drift.sh` appended to the `run_m65_suite` loop; `selftest m65-suite` usage prose names the new committed==regenerated AGENTS.md regen-drift pin. In-scope, edit / enforcement class / `landmark_authorized: true`. Staged bytes byte-identical to the prior two verified candidates.
- `docs/MILESTONES.md` (+63/−0 pure append, STAGED): one `## v1.1.4` entry. In-scope, edit / release class / `landmark_authorized: true`. Staged bytes byte-identical to prior.
- `tests/fixtures/m6.5/test-agents-md-drift.sh` (NEW, 152 lines, STAGED as add): standalone hermetic AGENTS.md committed==regen drift suite, now with the Rev 4 name-pin (grew from 130 → 152 lines, +22 for the name-pin block + F8 header). In-scope, create / ordinary.

(Crosscheck note: out-of-band and correctly OUTSIDE the lock and the staged set — `.codex/config.toml` (pre-existing model-config edit, unstaged, exempt) and the governance/evidence records `.harness/plans/dmc-fable-core-regen-pin.md`, `.harness/evidence/dmc-fable-core-regen-pin-critic-r{1,2,3,4}.json`, prior verification reports under `.harness/verification/`, and the preserved `release-readiness.json` files under the run dirs. None are staged; the staged set is exactly the 3 candidate paths.)

## Commands Run

| Command | Result | Reason | Output Summary |
|---|---|---|---|
| `git show :tests/fixtures/m6.5/test-agents-md-drift.sh` | PASS | staged suite == Rev 4 name-pin design | see Manual Checks / suite-content rows |
| `git diff --cached --name-only` / `--numstat` | PASS | staged set == exactly 3; candidate landmarks unchanged | `bin/dmc 3/2`, `docs/MILESTONES.md 63/0`, `test-agents-md-drift.sh 152/0` |
| `bash tests/fixtures/m6.5/test-agents-md-drift.sh` | PASS | name-pin + positive byte-equality + both negatives + hermetic | `RESULT: 9 PASS / 0 FAIL`, exit 0; PORCELAIN-STABLE |
| `bin/dmc selftest m65-suite` | PASS | pin wired into the blocking M6.5 group | `M65-EXIT=0`; line-anchored actual `[FAIL]` records = 0; RESULT lines `test-codex-shims.sh 143/0`, `test-skills-mirror.sh 19/0` (+`[skills-mirror] 7/0`), `test-agents-md.sh 35/0`, `test-agents-md-drift.sh 9/0`; PORCELAIN-STABLE |
| `bin/dmc agents-md --root . --stdout \| diff - AGENTS.md` | PASS | AGENTS.md neutrality | `AGENTS-NEUTRAL-EMPTY` |
| `bash .claude/install/dmc-install.sh --emit-manifest \| diff - INSTALL_MANIFEST.md` | PASS | INSTALL_MANIFEST neutrality | `MANIFEST-NEUTRAL-EMPTY` |
| read `AGENTS.md:1` + `bin/lib/dmc-agents-md.py:173-174` | PASS | root-cause confirmation | `# AGENTS.md — DMC`; `name = os.path.basename(os.path.abspath(root))` |
| read run-1 + run-2 `release-readiness.json` | PASS | supersession evidence | run-1 overall FAIL (G2); run-2 overall PASS, 9/9 sub-gates |
| `git rev-parse HEAD` | PASS | HEAD == compiled_at_head | `bbeb277…` |

Carried forward from the prior passes (candidate `bin/dmc`/`MILESTONES` bytes unchanged): `bash -n bin/dmc` = SYNTAX-OK; `selftest m8-suite` = 83/17/16/10 (INSTALL_MANIFEST byte-equality pin re-affirmed); `selftest agents-md` = 27/0; CI `.github/workflows/dmc-ci.yml` = blocking `selftest m65-suite` at `:172-173`, advisory `selftest --all` at `:191-194`, `.github/` absent from the candidate. Deliberately NOT run in this read-only lane: clean-tree `selftest --all` and `gate release --full` (orchestrator lane — PENDING rows below). Guard note: one probe was DENIED by the armed bash-radius classifier for containing `2>/dev/null` (a `>` write target); re-run clean — enforcement is live.

## Manual Checks

| Check | Result | Notes |
|---|---|---|
| plan sha256 triple-match (plan == run.json == scope.lock == critic r4) | PASS | all `d0156d70…`; critic r4 APPROVE, advisory, fresh, 0 blockers |
| operative-snapshot integrity | PASS | computed `scope.lock.json` `cad786d7…` == run.json `scope_lock_sha256`; `snapshot.txt` `867dee44…` == run.json `snapshot_sha256` |
| `compiled_at_head` == HEAD | PASS | `bbeb27702d23892a0482127996552f393b1fddad` (post soft-reset of `8451cc0`) |
| scope.lock `files[]` == exactly 3 paths, correct grants | PASS | bin/dmc edit/enforcement/`landmark_authorized`; docs/MILESTONES.md edit/release/`landmark_authorized`; test create/ordinary |
| staged set == the same 3 lock paths; nothing else staged | PASS | `git diff --cached --name-only` == the 3; `.codex/config.toml` unstaged |
| bounds honored (3 files / 500 added / 60 deleted) | PASS | 3 files / 218 added (3+63+152) / 2 deleted — within 3/500/60 |
| working tree == index for the suite | PASS | `git diff --numstat -- <suite>` empty |
| suite name-pinned copy INCL `.git`, to fixed-basename `$TMP/DMC` | PASS | `PINNED="$TMP/DMC"; cp -R "$ROOT" "$PINNED"`; asserts `$PINNED/.git` exists (check-ignore landmark-walk parity) |
| regen from the copy, NOT `--root "$ROOT"` | PASS | `"$DMC" agents-md --root "$PINNED" --stdout` — binary under test stays the repo's own `bin/dmc`; only `--root` is name-pinned |
| compare vs the REAL on-disk committed AGENTS.md | PASS | `cmp -s "$REGEN" "$COMMITTED"`, `COMMITTED="$ROOT/AGENTS.md"` — NOT compare-minus-line-1, NOT clone-of-HEAD |
| negative controls + porcelain hermetic semantics unchanged | PASS | (3) one-byte-of-a-COPY, (4) regen-vs-section-deleted-COPY, (5) porcelain before/after DELTA — byte-identical to the Rev-3 suite |
| header documents F8 + `cp` in the subprocess set | PASS | F8 NAME-PIN block present; "no subprocess beyond bin/dmc, git status --porcelain, and coreutils (incl. cp -R for the name-pin working-tree copy) in mktemp" |
| +1 name-pin PASS record (RESULT now 9) | PASS | new record "name-pin: working-tree copy at DMC-named dir incl. .git (check-ignore parity)" |
| `bin/dmc` + `docs/MILESTONES.md` staged bytes unchanged vs prior verification | PASS | `git diff --cached --numstat` = `bin/dmc 3/2`, `docs/MILESTONES.md 63/0` (identical) |
| generator `dmc-agents-md.py` OUT of scope + byte-untouched | PASS | fix is in the suite, not the generator (correct per critic r4); not in the candidate |
| PINNED_BASELINE (`dmc-legacy-selftest.py`) byte-untouched | PASS | not in the staged set or diff |
| no frozen `dmc-v0.*` tool / `.github/` / m8 manifest test in the candidate | PASS | staged set is exactly the 3 in-scope paths |
| HEAD unchanged; verifier read-only | PASS | HEAD `bbeb277`; no commit/push; I staged nothing (orchestrator staged the candidate) |

## Scope Review

Result: PASS

Notes: staged set == the exact 3 in-scope paths; lock == the same 3. Only the suite grew (name-pin block); `bin/dmc` and `docs/MILESTONES.md` staged bytes are identical to the prior verified candidate. Bounds honored (3 files / 218 added / 2 deleted, within 3/500/60). Generator `dmc-agents-md.py` remains OUT of scope and byte-untouched (the fix is in the suite, not the generator — correct per r4). No G4 override needed. `bin/dmc` + `docs/MILESTONES.md` are enforcement/release landmarks → the gate's landmark-flag is its call (run-1 FLAG, run-2 CLEAR; either is PASS-compatible).

## Package / Env / Migration Review

Package files changed: no
Env files changed: no
Migration files changed: no

Notes: No dependency manifests, lockfiles, or migrations touched. No `.env`/secret read or altered. The only non-source out-of-band file is `.codex/config.toml` (pre-existing, out-of-scope, unstaged).

## Acceptance Criteria Coverage

| # | Criterion | Observed Evidence | Verdict |
|---|---|---|---|
| 1 | AGENTS.md committed==regen pin wired + GREEN, incl. NEW rename-decoupling line | `selftest m65-suite` runs `test-agents-md-drift.sh` 9/0 incl. the name-pin assertion; positive byte-equality PASS; `selftest agents-md` 27/0 | Wired + green MET; the rename-decoupling (name-independence in a non-DMC dir) proof is PENDING-POST-COMMIT-REPLICA (live tree is dir `DMC`, structurally cannot distinguish) |
| 2 | Pin has teeth (negative controls) | (3) one-byte caught, (4) regen-vs-section-deleted still FAILS (non-vacuous), (5) porcelain byte-identical before/after | MET |
| 3 | INSTALL_MANIFEST committed==regen re-affirmed | `m8-suite` `test-manifest-drift.sh 10/0` (carried forward; candidate `bin/dmc` unchanged) + manifest neutrality diff EMPTY | MET |
| 4 | Legacy 802/3/3 aggregate EXACT + replica --all rc=0 | `PINNED_BASELINE` byte-untouched (not in candidate) | PINNED_BASELINE-untouched MET; replica `--all` rc=0 + `tools=49 PASS=802 FAIL=3 N/A=3` EXACT is PENDING-POST-COMMIT-REPLICA (authoritative name-independence + aggregate proof runs in a fresh NON-DMC-named replica after recommit) |
| 5 | Derived artifacts neutral | both neutrality diffs EMPTY | MET |
| 6 | CI picks up the pin, no workflow edit | blocking `selftest m65-suite` (`:172-173`) iterates the drift suite; `.github/` not in candidate; the name-pin specifically keeps this blocking step green under CI's `Do-Me-Coding` checkout dir | MET |
| 7 | Scope + gate + ceiling | lock == staged == exactly 3 (VERIFIED); bounds honored; no G4 override; nothing committed/pushed | Scope/lock/staged precondition MET; `gate release --full --run-id dmc-run-af50706d0402` is PENDING-POST-STAGING (orchestrator lane) |

## Unresolved Risks

- AC-1 rename-decoupling proof + AC-4 replica `selftest --all` (rc=0 AND `802/3/3` EXACT) are PENDING-POST-COMMIT-REPLICA — the orchestrator must run a fresh replica in a NON-`DMC`-named dir after the recommit; that leg is the authoritative name-independence proof (the live tree is itself named `DMC` and structurally cannot distinguish, which is exactly why the run-2 replica caught the defect the live lanes missed). Record it in the build evidence with the final post-amend SHA.
- `gate release --full --run-id dmc-run-af50706d0402` (AC-7) is PENDING-POST-STAGING — precondition (lock == staged == 3) satisfied; expected overall PASS. (Its `approvals` CF2 sub-gate re-runs this same instance validator on the referenced verification_ref — this corrected report restores the canonical `Changed Files` + `Manual Checks` headers so that validation now ACCEPTs.)
- Both prior honest failure records preserved (run-1 `release-readiness.json` G2 FAIL; run-2 replica catch in plan F8 + Notes Rev 4). Amending unpushed local `8451cc0` is governance-clean (no remote contains it; LOCAL-commit ceiling honored).
- Push / CI / main-FF remain a separate human gate.

## Final Status

PASS — bound to `dmc-run-af50706d0402`. The Rev 4 name-pin fix is present in the staged suite, correctly layered (suite-only; generator untouched), and re-affirmed green (drift suite 9/0, m65 exit 0, neutrality EMPTY, porcelain stable). AC-1 rename-decoupling + AC-4 replica `--all`/aggregate are PENDING-POST-COMMIT-REPLICA; AC-7 gate is PENDING-POST-STAGING. Every check available to the read-only verifier lane is GREEN; no critical verification failed; both prior honest failures are preserved. This instance restores the canonical `Changed Files` + `Manual Checks` sections so it validates against `dmc.verification-instance.v1`.
