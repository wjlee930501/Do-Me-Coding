# Verification Report

## Run ID

dmc-run-6e707694161f

## Plan

`.harness/plans/dmc-fable-core-replica-default.md` (v1.1.6 — committed-replica default for the `dmc selftest --all` legacy leg; Rev 2).

Binding chain (all bound to the same plan, independently recomputed):
- plan sha256 `b33f355c7abfeadb3b4e498e79b7cd29e1ccb6f6cece1611843f3c1b9c8725fb` (recomputed via `shasum -a 256`) == run.json `plan_hash` == scope.lock `plan_hash` == critic r2 `plan_hash`. MATCH.
- `compiled_at_head` = `92371db12826658bb4f8dc0922a0266f73cb10bf` == `git rev-parse HEAD` == HEAD `92371db`, branch `claude/dmc-fable-core`. MATCH.
- scope.lock file sha256 `9703cf4e2553dc77bda8096464c1bdc89f0232ff6907d4c88cfbcf5bd18a152d` == run.json `operative_snapshot.scope_lock_sha256`. MATCH.
- scope.lock `repo_hash` `e3955580…` == run.json `repo_hash`. MATCH.
- scope.lock `files[]` = EXACTLY 4: `bin/dmc` (edit, landmark_authorized, class=enforcement), `docs/DMC_V1_HONEST_SCOPE.md` (edit, class=ordinary), `docs/MILESTONES.md` (edit, landmark_authorized, class=release), `tests/install/test-selftest-replica-default.sh` (create, class=ordinary). bounds max_files=4 / max_added=400 / max_deleted=40 — all respected.
- critic r2 `repo_hash` = `497ca4b` (pre-v1.1.5). VERIFIED-CONSISTENT, not a mismatch: critic r2 pre-approved the rebase-free arm after the DISJOINT v1.1.5 commit (its `r1-load-bearing-carryforward` criterion cites "v1.1.5 disjointness"); the plan_hash binds the artifact, and it matches. Verdict APPROVE, advisory=true, blockers=[].

## Changed Files

Tracked (git diff --numstat), in scope:
- `bin/dmc` +83/−7 — `run_legacy_selftest_all()` helper + `--in-place` pre-scan + line-565 redirect + `legacy-all` target + usage text.
- `docs/DMC_V1_HONEST_SCOPE.md` +1/−1 — one clarifying clause at S5.
- `docs/MILESTONES.md` +60/0 — append-only v1.1.6 entry.

Untracked, in scope:
- `tests/install/test-selftest-replica-default.sh` (new hermetic C1–C5 + Z).

Exempt (not attributable to this cycle, correctly untouched by scope):
- `.codex/config.toml` +4/0 — PRE-EXISTING dirty file (present in session-start git status); the very dirt this cycle immunizes the leg against.
- Untracked `.harness/evidence/*`, `.harness/plans/*`, `.harness/verification/*` — governance records.

Frozen surfaces UNTOUCHED (confirmed by `git diff --name-only`): no `bin/lib/dmc-v0.*`, no `dmc-legacy-selftest.py`, no `PINNED_BASELINE`, no `.harness/evidence/*` mirror, no `.github/`. `bin/dmc mirror-check` → `RESULT: PASS mirror-check green` (55-file pinned set, no stray copies).

## Commands Run

1. `shasum -a 256 .harness/plans/dmc-fable-core-replica-default.md` → `b33f355c…` (binding chain).
2. `bash -n bin/dmc` → `SYNTAX_OK bin/dmc`; `bash -n tests/install/test-selftest-replica-default.sh` → `SYNTAX_OK`.
3. `git diff --numstat` → `83 7 bin/dmc` / `1 1 docs/DMC_V1_HONEST_SCOPE.md` / `60 0 docs/MILESTONES.md` / `4 0 .codex/config.toml`.
4. `bin/dmc mirror-check` → `RESULT: PASS mirror-check green`.
5. `bin/dmc linkcheck` → `OK: linkcheck clean — 24 file(s) scanned…`, `LINKCHECK_RC=0`.
6. `bash tests/install/test-selftest-replica-default.sh` (hermetic, serialized legacy leg) → RESULT line: `==== selftest replica-default test: 15 passed, 0 failed ====`. C1 aggregate assertion PASS: `C1: aggregate EXACT tools=49 PASS=802 FAIL=3 N/A=3` (+ `C1: SELFTEST-ALL RESULT PASS`, `C1: default legacy-all exits 0 despite dirty tree + passive mode`). C2 tree FLIP PASS (replica V15 `[PASS]` / in-place `[FAIL]`). C3 mode FLIP PASS (replica npm-ask PASS / in-place-passive FAIL). C4 flag-plumbing PASS. C5 fail-loud PASS (nonzero + distinct FATAL + no 802 + no RESULT). Z PASS (real-repo porcelain byte-identical).
7. `bin/dmc selftest legacy-all` (LIVE, default committed-replica, dirty `.codex/config.toml` present) → `-- aggregate: tools=49 PASS=802 FAIL=3 N/A=3 timeouts=0 unparsed=0 --`, `PASS aggregate == pinned baseline exactly`, rollback `RESULT: PASS`, `==== SELFTEST-ALL RESULT: PASS ====`, `LEGACY_ALL_RC=0`. This is the headline: the live dirty tree no longer flips the baseline.
8. `bin/dmc selftest legacy-all --in-place` (LIVE hatch, serialized after #7, mode active) → `-- aggregate: tools=49 PASS=801 FAIL=4 N/A=3 timeouts=0 unparsed=0 --`, `FAIL aggregate DRIFTED from pinned baseline`, `==== SELFTEST-ALL RESULT: FAIL ====`, `INPLACE_RC=1`. EXPECTED: `--in-place` targets the live working tree, so the tree-coupled `dmc-v0.6.0` V15 drifts on the dirty `.codex/config.toml` (one tool PASS→FAIL: 802→801, 3→4; only one because mode=active, so the mode-coupled tool does NOT flip). This is the hatch working correctly and the clean live FLIP proof (default 802/3/3 vs in-place 801/4/3), NOT a regression.
9. `bin/dmc selftest m65-suite` → `test-codex-shims.sh: 161 PASS / 0 FAIL`; `test-skills-mirror.sh RESULT: 19 PASS / 0 FAIL`; `test-agents-md.sh RESULT: 35 PASS / 0 FAIL`; `test-agents-md-drift.sh RESULT: 9 PASS / 0 FAIL`; `M65_RC=0`; every "real repo byte-identical" self-check PASS.
10. Neutrality (derived-artifact) diffs, via pipes: `bin/dmc agents-md --root . --stdout | diff - AGENTS.md` → EMPTY (`AGENTS_DIFF_RC=0`); `bash .claude/install/dmc-install.sh --emit-manifest | diff - INSTALL_MANIFEST.md` → EMPTY (`MANIFEST_DIFF_RC=0`).
11. `git status --porcelain` before/after the entire battery → byte-identical (the 4 in-scope edits + the same untracked governance records + the new test); no run left dirt.

Serialization honored: the three legacy legs (#6 C1 full leg, #7, #8) ran one-at-a-time in blocking/background-with-wait calls; never two concurrently. m65-suite (#9) is not a legacy leg.

## Manual Checks

Code review of `bin/dmc` `run_legacy_selftest_all()` (lines ~344–397 + dispatch 611–703):
- (a) DEFAULT path clones `git clone --no-hardlinks --quiet "$src" "$replica/repo"` into `mktemp -d`; `git -C … remote remove origin` severs write-back; HEAD-sha guard asserts `rep_head` non-empty AND `src_head == rep_head` else FATAL+return 1; invokes the REPLICA's own `python3 "$replica/repo/bin/lib/dmc-legacy-selftest.py" selftest-all`. CONFIRMED.
- (b) FAIL-LOUD on every provisioning failure (git absent / not-a-work-tree / mktemp fail / clone fail / HEAD mismatch) — each prints a distinct `FATAL: selftest --all replica provisioning failed: <reason>` on stderr and `return 1`. NO code path falls back to the in-place run on failure. CONFIRMED (the only in-place path is gated by explicit `SELFTEST_IN_PLACE=1`).
- (c) `--in-place` branch is byte-identical to the historical invocation (`python3 "$LCORE" selftest-all; return "$?"`). The `set --` rotate pre-scan (lines 616–625) is bash-3.2 safe and traced correct for `--all --in-place`, `--in-place --all`, `legacy-all --in-place`, bare `--all`, and no-arg. CONFIRMED.
- (d) `legacy-mirror` leg (lines 637 in `--all`, 676 in target loop) is textually unchanged and stays in-place — frozen bin/lib↔evidence equality still verified each run. CONFIRMED.
- (e) `trap 'rm -rf "$replica" 2>/dev/null' EXIT` belt + inline `rm -rf` with `trap - EXIT` on every return path. CONFIRMED.
- (f) focused `legacy-all` target (line 677) routes through the same helper. CONFIRMED.
- (g) NO `.harness/mode` copy — only `git clone`; mode is gitignored ⇒ absent in replica ⇒ active default (the mode normalization). CONFIRMED.
- C4's `unknown target: badtarget` path exists at line 700. CONFIRMED.

Docs review:
- `DMC_V1_HONEST_SCOPE.md` clause now makes the "committed-replica scoped" claim TRUTHFUL — states the default `--all`/`legacy-all` auto-materializes the replica (clone HEAD into a severed mktemp dir), and `--in-place` reproduces the historical live-tree misread. Aligns doc with tool behavior; no over-claim.
- `MILESTONES.md` v1.1.6 entry is append-only, house style; records the retired rituals (manual committed-replica clone, `.codex/config.toml` stash, mode-restore dance), the `--in-place` hatch, "legacy 802/3/3 UNCHANGED (no masking)", and the "push/CI/main-FF: human gate" line.

Executor deviation ruling — ACCEPTABLE. The executor omitted the OPTIONAL full-leg `--in-place` belt. This was a critic-r2 ADVISORY (non-blocking) suggestion, not a plan AC requirement: plan AC C4's stated verification method IS the C2/C3 in-place arms (both green in the hermetic suite), plus the C4 flag-plumbing probe. I additionally executed the full live `--in-place` leg myself (command #8) → 801/4/3 FAIL, which closes that advisory belt and the plan's Verification-Command line 158 with first-hand deterministic evidence. No enforcement gap; the hatch is proven real at both the tool level (C2/C3) and the full-leg level (#8).

## Scope Review

Result: PASS

Notes: The tracked delta is EXACTLY the 3 authorized edits (`bin/dmc`, `docs/DMC_V1_HONEST_SCOPE.md`, `docs/MILESTONES.md`) plus the 1 authorized create (`tests/install/test-selftest-replica-default.sh`) — matching scope.lock `files[]` 1:1 and within all diff bounds. `.codex/config.toml` is pre-existing dirt (not this cycle's edit); untracked `.harness` governance records are exempt. No frozen `dmc-v0.*` / `dmc-legacy-selftest.py` / `PINNED_BASELINE` / `.github/` / evidence-mirror change. mirror-check green.

## Package / Env / Migration Review

Package files changed: none
Env files changed: none
Migration files changed: none

Notes: `.codex/config.toml` is a pre-existing, out-of-scope working-tree modification, not a package/env manifest introduced by this cycle; no `.env*` touched or read.

## Unresolved Risks

- PENDING-POST-COMMIT: the committed-replica full-battery `bin/dmc selftest --all` at the post-commit sha (orchestrator step after the local commit). The full battery is exercised here at the pre-commit sha via the hermetic C1 leg (802/3/3 EXACT) and the two live legs; expected PASS once committed.
- PENDING-POST-STAGING: `bin/dmc gate release --full --run-id dmc-run-6e707694161f` (orchestrator, after staging). Expected PASS — landmark-authorized FLAG on `bin/dmc` and the release-class `docs/MILESTONES.md` (append-only, non-degrading), no G4 protected-path override in play.
- Residual (documented, out of scope by design): env-var coupling (leaked-provider-key 801/4/3 class) is NOT fixed — the replica inherits parent env; HONEST_SCOPE scopes the fix to tree+mode only. Accepted.

## Final Status

PASS

All 9 plan Acceptance Criteria are satisfied by observed deterministic evidence (C1 802/3/3 EXACT ×2 independent observations; C2 tree FLIP; C3 mode FLIP; C4 hatch real — C2/C3 in-place arms + plumbing + live 801/4/3; C5 fail-loud; mirror-integrity unweakened; Z real-repo byte-identical; frozen mirrors + PINNED_BASELINE byte-untouched; linkcheck exit 0). Binding chain intact, scope clean, no masking, hermetic. Recommend proceeding to local commit; push / CI / main-FF remain the human gate. Two PENDING checks are post-commit/post-staging orchestrator steps, expected PASS.
